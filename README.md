Billboard_live_editor
=====================

billboard WYSIWYG editor, captures a client side screenshot

Uses a number of open source libraries to mock up a site that allows 
a user to edit text and add an image to a pre-defined location on the board. 
Then it uses the Canvas element to encode the Mocked up HTML as a JPEG for publishing
to a billboard.
